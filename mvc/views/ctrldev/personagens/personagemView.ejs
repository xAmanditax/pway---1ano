<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alterar Personagens</title>
</head>
<body>
    <main>

        <section id="personagemView">
            <h2>Alterar Personagens</h2>
            <form action="" id="enviarAtualizacao">
                <input aria-hidden="true" type="hidden" name="id" id="id" value="<%= r.id %>">
                <div class="row">
                    <div class="col">
                        <label for="txtnomeperson" class="form-label">Personagem</label>
                        <input type="text" value="<%= r.nome %>" name="txtnomeperson" id="txtnomeperson" class="form-control" tabindex="1">
                    </div>
                    <div class="col">
                        <label for="txtgenperson" class="form-label">Genero</label>
                        <input type="text" value="<%= r.genero %>" name="txtgenperson" id="txtgenperson" class="form-control" tabindex="2">
                    </div>
                </div>
                <div class="row">
                    <div class="col">
                        <label for="txttipoperson" class="form-label">Tipo</label>
                        <input type="text" value="<%= r.tipo %>" name="txttipoperson" id="txttipoperson" class="form-control" tabindex="3" >
                    </div>
                    <div class="col">
                        <label for="txttotmoedaperson" class="form=label">Total de Moedas</label>
                        <input type="text" value="<%= r.totalMoeda %>" name="txttotmoedaperson" id="txttotmoedaperson" class="form-control" tabindex="4">
                    </div>
                </div>
                <div class="row">
                    <div class="col">
                        <label for="txtlatperson" class="form-label">Latitude</label>
                        <input type="text" value="<%= r.latitude %>" name="txtlatperson" id="txtlatperson" class="form-control" tabindex="5">
                    </div>
                    <div class="col">
                        <label for="txtlongperson" class="form-label">Longitude</label>
                        <input type="text" value="<%= r.longitude %>" name="txtlongperson" id="txtlongperson" class="form-control" tabindex="6">

                    </div>
                    <div class="col">
                        <div class="row"><input type="submit" value="Alterar" id="btAlter" name="bAlter" class="btn btn-warning mt-4" tabindex="7"></div>
                    </div>
                </div>
            </form>
    </section>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    <script>
     document.getElementById('enviarAtualizacao').addEventListener('submit', (e) => {
         e.preventDefault()
         enviar()
     })

     async function enviar(){
            const id= document.getElementById('id').value
            const nome= document.getElementById('txtnomeperson').value
            const genero = document.getElementById('txtgenperson').value
            const tipo = document.getElementById('txttipoperson').value
            const totalMoeda= document.getElementById('txtotmoedaperson').value
            const latitude = document.getElementById('txtlatperson').value
            const longitude = document.getElementById('txtlongperson').value

            const data = {
                'nome': nome,
                'genero': descricao,
                'tipo': tipo,
                'totalMoeda': totalMoeda,
                'latitude': latitude,
                'longitude': longitude,
                'id': id
        }

        const dados = await fetch ('/person/'+id, {
            method: 'PUT',
            headers: {
                'Accept': 'application/json',
                'Content-Type': 'application/json'
            },

            body: JSON.stringify(data)
        })

        const json = await dados.json()
        let personagens = await json

        alert(personagens.msg)
    }


     </script>
</body>
</html>